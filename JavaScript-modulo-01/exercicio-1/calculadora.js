const a = parseFloat(prompt("Digite um valor:"))
const b = parseFloat(prompt("Digite outro valor:"))

const soma = a + b
const subtracao = a - b
const multiplicacao = a * b
const divisao = a / b

alert(
    "Resultados: \n" +
    "\nSoma: " + soma +
    "\nSubtração: " + subtracao +
    "\nMultiplicação: " + multiplicacao +
    "\nDivisão: " + divisao
)